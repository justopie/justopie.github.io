(defun _expandNumbers (intStart intEnd / intMiddle lstNumbers)
  (setq intMiddle (1+ intStart))
  (while (< intMiddle intEnd)
    (setq lstNumbers (append lstNumbers (list intMiddle))
	  intMiddle  (1+ intMiddle)
    )
  )
  (setq lstNumbers (append (list intStart) lstNumbers (list intEnd)))
)